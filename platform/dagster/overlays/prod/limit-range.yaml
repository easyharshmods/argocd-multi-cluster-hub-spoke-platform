apiVersion: v1
kind: LimitRange
metadata:
  name: dagster-limits
spec:
  limits:
  - default:
      cpu: "1"
      memory: 2Gi
    defaultRequest:
      cpu: 250m
      memory: 512Mi
    type: Container
